rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // All uploaded images are publicly readable (used in public website)
    // Only authenticated admin users can write or delete
    match /{allPaths=**} {
      allow read: if true;
      allow write, delete: if request.auth != null;
    }
  }
}
